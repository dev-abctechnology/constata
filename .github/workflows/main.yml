name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ... (outros passos aqui)

      # Corrigindo os comandos echo e encadeando-os usando &&
    - run: echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> ${{ env.PROPERTIES_PATH }}
    - run: echo "storePassword=${{ secrets.KEY_PASSWORD }}" >> ${{ env.PROPERTIES_PATH }}
    - run: echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> ${{ env.PROPERTIES_PATH }}


      # ... (outros passos aqui)
